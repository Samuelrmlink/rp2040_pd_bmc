#include "main_i.h"
/*
void pe_transmit(txFrame *txf, bmcChannel *ch) {
    // TODO : implement transmission retries
    pdf_transmit(txf, ch);
    free(txf->out);
}


void policy_engine_sop_frame_handler(pd_frame *in_pdf, txFrame *out_txf, pd_frame *latestSrcCap, peOptions *peOpt) {
    switch(pdf_get_sop_msg_type(in_pdf)) {
        case (controlMsgGoodCrc) :
            pdf_generate_goodcrc(in_pdf, out_txf);
            printf("Good crc generation :D\n");
            break;
        default :
            printf("Frame Handler error\n");
    }
}
*/
